# Rive Animation 仕様書
## pikotan_animation.riv 解析結果

### 基本情報
- **ファイル**: `assets/animations/pikotan_animation.riv`
- **Artboard**: "Artboard"
- **サイズ**: 1024.0 x 1024.0
- **総アニメーション数**: 22個
- **ステートマシン数**: 5個

---

## 利用可能なアニメーション一覧

### 🛌 Sleep系アニメーション
- `sleep_A` - 睡眠アニメーションA
- `sleep_B` - 睡眠アニメーションB  
- `sleep_C` - 睡眠アニメーションC
- `sleep_D` - 睡眠アニメーションD

### 🚶 歩行アニメーション
- `walk_L` - 左歩行
- `walk_R` - 右歩行

### 😴 待機アニメーション
- `idle` - 基本待機（前後の動きを含む）
- `idle_2` - 待機バリエーション
- `eye` - 瞬きアニメーション

### 🎌 フラグアニメーション
- `flag_idle` - フラグ待機状態
- `_flag_pose` - フラグポーズ

### ✅❌ 正解・不正解アニメーション
- `corrent_start` - 正解開始
- `corrent_end` - 正解終了
- `corrent_roop` - 正解ループ
- `incorrent_start` - 不正解開始
- `incorrent_end` - 不正解終了
- `incorrent_roop` - 不正解ループ

### 🎰 ステートマシンアニメーション
- `Sleep_State_Machine` - 睡眠ステートマシン
- `Idle_State_Machine` - 待機ステートマシン
- `Flag_State_Machine` - フラグステートマシン
- `Walk_R_State_Machine` - 右歩行ステートマシン
- `Walk_L_State_Machine` - 左歩行ステートマシン

---

## ステートマシン詳細

### 1. Sleep_State_Machine
- **入力パラメータ**: 0個
- **用途**: 自動睡眠アニメーション

### 2. Idle_State_Machine  
- **入力パラメータ**: 0個
- **用途**: 基本待機状態（前後の動きはここ）

### 3. Flag_State_Machine ⭐
- **入力パラメータ**: 2個
  - `incorrent_start` (StateMachineTrigger)
  - `corrent_start` (StateMachineTrigger)
- **用途**: インタラクティブな正解・不正解反応

### 4. Walk_R_State_Machine
- **入力パラメータ**: 0個
- **用途**: 右歩行アニメーション

### 5. Walk_L_State_Machine
- **入力パラメータ**: 0個  
- **用途**: 左歩行アニメーション

---

## 実装パターン

### 🎯 基本実装（現在使用中）
```dart
RiveAnimation.asset(
  'assets/animations/pikotan_animation.riv',
  animations: ['idle', 'walk_L', 'walk_R', 'sleep_A', 'flag_idle'],
  fit: BoxFit.contain,
)
```

### 🎮 インタラクティブ実装（Flag State Machine）
```dart
RiveAnimation.asset(
  'assets/animations/pikotan_animation.riv',
  stateMachines: ['Flag_State_Machine'],
  onInit: (artboard) {
    final controller = StateMachineController.fromArtboard(artboard, 'Flag_State_Machine');
    if (controller != null) {
      artboard.addController(controller);
      final correctTrigger = controller.findSMI<SMITrigger>('corrent_start');
      final incorrectTrigger = controller.findSMI<SMITrigger>('incorrent_start');
      
      // 使用例
      correctTrigger?.fire(); // 正解アニメーション実行
      incorrectTrigger?.fire(); // 不正解アニメーション実行
    }
  },
)
```

### 🔄 単一アニメーション実装
```dart
// 特定のアニメーションのみ再生
RiveAnimation.asset(
  'assets/animations/pikotan_animation.riv',
  animations: ['idle'], // または ['walk_L'], ['sleep_A'] など
)
```

---

## 推奨使用シナリオ

### 🏠 ホーム画面
- **アニメーション**: `['idle', 'eye']`
- **説明**: 静かな待機状態で親しみやすさを表現

### 📚 学習画面
- **アニメーション**: `['idle', 'walk_L', 'walk_R']`
- **説明**: 学習への意欲を歩行で表現

### 🎯 結果表示
- **ステートマシン**: `Flag_State_Machine`
- **トリガー**: 正解時 `corrent_start`、不正解時 `incorrent_start`
- **説明**: ユーザーの回答に応じた感情表現

### 😴 休憩時間
- **アニメーション**: `['sleep_A', 'sleep_B', 'sleep_C', 'sleep_D']`
- **説明**: リラックス状態を表現

---

## 技術的注意事項

### ⚠️ 既知の制限
- ステートマシンの動的切り替えでエラーが発生することがある
- 複数アニメーション同時再生は安定している
- トリガー制御は慎重な実装が必要

### ✅ 推奨実装方法
1. **基本**: 複数アニメーション同時再生
2. **高度**: 単一ステートマシンでトリガー制御
3. **組み合わせ**: Riveアニメーション + カスタムTransform効果

### 🎨 カスタム効果との組み合わせ
```dart
// Riveアニメーション + 浮遊効果
Transform.translate(
  offset: Offset(0, floatingValue),
  child: Transform.scale(
    scale: scalingValue,
    child: RiveAnimation.asset(...),
  ),
)
```

---

## 現在の実装状況

### ✅ ホーム画面
- **使用アニメーション**: Riveアニメーション（複数種類）
- **追加効果**: なし
- **状態**: 正常動作中

### ✅ インプットトレーニング画面
- **使用アニメーション**: `['idle', 'walk_L', 'walk_R', 'sleep_A', 'flag_idle']`
- **追加効果**: 浮遊 + 拡大縮小 + 光沢
- **状態**: 正常動作中

### 💡 拡張可能性
- スワイプ操作と連動したアニメーション切り替え
- 学習進捗に応じた感情表現
- 時間帯による自動アニメーション変更